# Point the app at a different Supabase project than the production one.
# Both are optional: without them the build talks to the live project.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=sb_publishable_your_key
